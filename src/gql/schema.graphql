type Post {
  id: ID!
  title: String!
  text: String!
  author: String!
}

type User {
  id: ID!
  username: String!
  name: String!
  email: String!
}

type Query {
  posts: [Post!]!
  users: [User!]!
}
