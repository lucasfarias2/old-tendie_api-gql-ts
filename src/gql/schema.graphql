type Post {
  id: ID!
  title: String!
  text: String!
  author: String!
}

type User {
  id: ID!
  email: String!
  username: String!
  name: String!
}

type Query {
  posts: [Post!]!
  users: [User!]!
}
